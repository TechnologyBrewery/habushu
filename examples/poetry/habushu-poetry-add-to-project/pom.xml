<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    
    <parent>
        <groupId>org.technologybrewery.habushu</groupId>
        <artifactId>poetry</artifactId>
        <version>3.0.0-SNAPSHOT</version>
    </parent>

    <name>habushu::Examples::Poetry::Add Habushu to a project</name>
    <description>This example demonstrates how to add Habushu to a new or existing Poetry project</description>

    <artifactId>habushu-poetry-add-to-project</artifactId>

    <!-- set the Maven module packaging to habushu-->
    <!-- This allows us to build this Poetry project via the `mvn install` or `mvnd install` commands -->
    <packaging>habushu</packaging>

    <build>
        <directory>dist</directory>
        <!-- habushu-maven-plugin is inherited and activated via the examples/pom.xml -->
        <!-- See the Maven Build Directory Customization section of the habushu/README.md for details -->
    </build>

</project>
