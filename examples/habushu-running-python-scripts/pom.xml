<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.technologybrewery.habushu</groupId>
        <artifactId>examples</artifactId>
        <version>3.4.0</version>
    </parent>

    <name>habushu::Examples::Running Python Scripts</name>
    <description>Example of how to run Python commands using the run-command-in-virtual-env goal </description>

    <artifactId>habushu-running-python-scripts</artifactId>
    <packaging>habushu</packaging>

    <build>
        <directory>dist</directory>
        <plugins>
            <plugin>
                <groupId>org.technologybrewery.habushu</groupId>
                <artifactId>habushu-maven-plugin</artifactId>
                <configuration>
                </configuration>
                    <!-- Utilize the run-command-in-virtual-env goal to demonstrate how
                        commands/scripts may be executed via "run" and bound to the build
                        lifecycle phases -->
                <executions>
                    <execution>
                        <configuration>
                            <runCommandArgs>python -m grpc_tools.protoc -I=src
                                --python_out=src/habushu_running_python_scripts/generated src/person.proto
                            </runCommandArgs>
                        </configuration>
                        <id>generate-protobuf-bindings</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>run-command-in-virtual-env</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
